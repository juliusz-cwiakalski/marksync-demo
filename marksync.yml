version: 1

root: docs/

select:
  - docs/**/*.md

hierarchy: mirror

targets:
  default:
    type: confluence
    spaceKey: "39223300"
    parentPageId: "39223464"

sync:
  allowBranches:
    - main
  granularity: squash
  stalePlanMinutes: 15

render:
  mermaid:
    policy: render
    securityLevel: strict
    htmlLabels: false
    deterministicIds: true

output:
  format: storage
  color: auto

provenance:
  visiblePanel: true
